language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t jamesn8docker/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jamesn8docker/docker-react npm run test
